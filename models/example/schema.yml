
version: 2

models:
  - name: bronze_accounts
    description: "A starter dbt model"
    columns:
      - name: account_id
        description: "The primary key for this table"
        tests:
          - unique
          - not_null